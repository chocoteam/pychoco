.. _model:

Model
=====

The model is the core component of PyChoco. A model is created using the `Model()` constructor,
and it is the entry point to create variables, constraints, and solve problems.

.. autoclass:: pychoco.model.Model
   :members:
   :undoc-members:
   :noindex:

