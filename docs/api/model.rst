.. _model:

Model
=====

The model is the core component of PyChoco. A model is created using the `create_model` function,
and it is the entry point to create variables, constraints, and solve problems.

.. autofunction:: pychoco.create_model
   :noindex:

.. autoclass:: pychoco.model.Model
   :members:
   :undoc-members:
   :noindex:

